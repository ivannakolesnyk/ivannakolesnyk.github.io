import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#C39681",
      contrastText: "#000",
    },
    secondary: {
      main: "#F3EBE5",
      contrastText: "#fff",
    },
  },
});
